{
  "name": "@linux-meta/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "env -u FORCE_COLOR -u NO_COLOR PW_DISABLE_TS_ESM=1 playwright test",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "@playwright/test": "^1.60.0"
  }
}
